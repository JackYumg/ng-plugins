import { MainModalDirective } from './main-modal.directive';

describe('MainModalDirective', () => {
  it('should create an instance', () => {
    const directive = new MainModalDirective();
    expect(directive).toBeTruthy();
  });
});
